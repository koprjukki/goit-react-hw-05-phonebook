import React from "react";
import "./_customAlert.scss";

export const CustomAlert = () => {
	return (
		<div className="alert">
			<p className="alert-text">Contact already exsist!</p>
		</div>
	);
};
